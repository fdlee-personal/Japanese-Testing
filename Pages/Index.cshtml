@page
@model JPracticeWeb.Pages.IndexModel
@{
    ViewData["Title"] = "일본어 연습";
}

<section class="page-shell">
    <div class="card-panel">
        <p class="eyebrow">일본어 연습</p>
        <div class="button-row">
            <a class="action-button" asp-page="/Test">테스트 시작</a>
            <a class="action-button secondary-button" asp-page="/WordList">단어 목록</a>
        </div>
    </div>
</section>
