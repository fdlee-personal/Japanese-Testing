@page
@model JPracticeWeb.Pages.WordListModel
@{
    ViewData["Title"] = "단어 목록";
}

<section class="page-shell">
    <div class="card-panel">
        <h1 class="page-title page-title-sm">단어</h1>
        <form method="get" class="row g-3 align-items-end">
            <div class="col-12 col-md-4">
                <label for="word-type-filter" class="form-label">품사 필터</label>
                <select id="word-type-filter" name="type" class="form-select" onchange="this.form.submit()">
                    <option value="@JPracticeWeb.Pages.WordListModel.AllTypes" selected="@(Model.SelectedType == JPracticeWeb.Pages.WordListModel.AllTypes)">전체</option>
                    <option value="@JPracticeWeb.Models.TestClass.PartOfSpeechVerb" selected="@(Model.SelectedType == JPracticeWeb.Models.TestClass.PartOfSpeechVerb)">동사</option>
                    <option value="@JPracticeWeb.Models.TestClass.PartOfSpeechNoun" selected="@(Model.SelectedType == JPracticeWeb.Models.TestClass.PartOfSpeechNoun)">명사</option>
                    <option value="@JPracticeWeb.Models.TestClass.PartOfSpeechAdjective" selected="@(Model.SelectedType == JPracticeWeb.Models.TestClass.PartOfSpeechAdjective)">형용사</option>
                    <option value="@JPracticeWeb.Models.TestClass.PartOfSpeechAdverb" selected="@(Model.SelectedType == JPracticeWeb.Models.TestClass.PartOfSpeechAdverb)">부사</option>
                    <option value="@JPracticeWeb.Models.TestClass.PartOfSpeechExpression" selected="@(Model.SelectedType == JPracticeWeb.Models.TestClass.PartOfSpeechExpression)">표현</option>
                    <option value="@JPracticeWeb.Models.TestClass.PartOfSpeechOther" selected="@(Model.SelectedType == JPracticeWeb.Models.TestClass.PartOfSpeechOther)">기타</option>
                </select>
            </div>
            <div class="col-12 col-md-auto">
                <noscript>
                    <button type="submit" class="action-button secondary-button">적용</button>
                </noscript>
            </div>
        </form>
        @if (!string.IsNullOrWhiteSpace(Model.StatusMessage))
        {
            <p class="status-message">@Model.StatusMessage</p>
        }

        @if (Model.Words.Count == 0)
        {
            <p>저장된 단어가 없습니다. 먼저 단어를 추가하세요.</p>
        }
        else
        {
            var leftWords = Model.Words.Take(10).ToList();
            var rightWords = Model.Words.Skip(10).Take(10).ToList();

            <div class="word-columns">
                <div class="word-table-wrap">
                    <table class="word-table">
                        <thead>
                            <tr>
                                <th>단어</th>
                            </tr>
                        </thead>
                        <tbody>
                        @foreach (var item in leftWords)
                        {
                            <tr>
                                <td>
                                    <a class="word-link" asp-page="/WordDetail" asp-route-id="@item.Id" asp-route-p="@Model.CurrentPage" asp-route-type="@Model.SelectedType">
                                        @item.KoreanWord (@item.DictionaryTerm, @item.GetPartOfSpeechLabel())
                                    </a>
                                </td>
                            </tr>
                        }
                        </tbody>
                    </table>
                </div>

                @if (rightWords.Count > 0)
                {
                    <div class="word-table-wrap">
                        <table class="word-table">
                            <thead>
                                <tr>
                                    <th>단어</th>
                                </tr>
                            </thead>
                            <tbody>
                            @foreach (var item in rightWords)
                            {
                                <tr>
                                    <td>
                                        <a class="word-link" asp-page="/WordDetail" asp-route-id="@item.Id" asp-route-p="@Model.CurrentPage" asp-route-type="@Model.SelectedType">
                                            @item.KoreanWord (@item.DictionaryTerm, @item.GetPartOfSpeechLabel())
                                        </a>
                                    </td>
                                </tr>
                            }
                            </tbody>
                        </table>
                    </div>
                }
            </div>

            <div class="pager-row pager-row-compact">
                <div class="pager-cluster">
                    <a class="action-button secondary-button icon-button pager-icon @(Model.CurrentPage <= 1 ? "is-disabled" : string.Empty)"
                       asp-page="/WordList"
                       asp-route-p="@(Model.CurrentPage - 1)"
                       asp-route-type="@Model.SelectedType"
                       title="이전 페이지"
                       aria-label="이전 페이지">
                        <span aria-hidden="true">&#11013;&#65039;</span>
                    </a>
                    <span class="pager-info">@Model.CurrentPage / @Model.TotalPages 페이지 (@(Model.TotalCount)개)</span>
                    <a class="action-button secondary-button icon-button pager-icon @(Model.CurrentPage >= Model.TotalPages ? "is-disabled" : string.Empty)"
                       asp-page="/WordList"
                       asp-route-p="@(Model.CurrentPage + 1)"
                       asp-route-type="@Model.SelectedType"
                       title="다음 페이지"
                       aria-label="다음 페이지">
                        <span aria-hidden="true">&#10145;&#65039;</span>
                    </a>
                </div>
            </div>
        }

        <div class="button-row top-gap wordlist-actions">
            <a class="action-button secondary-button icon-button" asp-page="/Index" title="홈" aria-label="홈">
                <span aria-hidden="true">&#127968;</span>
            </a>
            <a class="action-button secondary-button icon-button" asp-page="/AddWord" title="단어 추가" aria-label="단어 추가">
                <span aria-hidden="true">&#10133;</span>
            </a>
        </div>
    </div>
</section>
